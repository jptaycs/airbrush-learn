{
  "name": "airbrush-learn-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/fetch-articles.mjs",
    "build": "npm run prebuild && astro build",
    "dev": "npm run prebuild && astro dev",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.0",
    "astro": "^4.15.0"
  },
  "devDependencies": {
    "@astrojs/tailwind": "^5.1.5",
    "@tailwindcss/typography": "^0.5.20",
    "tailwindcss": "^3.4.19"
  }
}
